---
import Section from "@/components/Section.astro";

import {education} from "@cv"
---

<Section title="Educación">
  {
    education.map(({institution, title, startDate, endDate}) => {
      const years = startDate + (endDate && " - " + endDate)
      return (
      <article>
        <header>
          <div>
          <h2>{title}</h>
          <h3>{institution}</h3>
          </div>
          <time>{years}</time>
        </header>
    </Arti>)
    }) 
  }
</Section>



<style>
  h2 {
    font-size: 1.1rem;
    color: #777;
    font-weight: 400;
    margin: 0;
  }

  h3 {
    font-size: .85rem;
    margin: 0 0 1rem 0;
    font-weight: 300;
  }

  ul {
    color: #555;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    list-style: disc inside;
    font-size: 0.9rem;
    text-indent: .6rem;
    margin: 1.8rem 0;
  }

  header {
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: flex-end;
  }

  time {
    color: #444;
    font-weight: 500;
    margin-bottom: 1rem;
  }
</style>


